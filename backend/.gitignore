# Dependencies
node_modules

# Environment & secrets
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime data / pid
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage/
.nyc_output/

# Build / dist
dist/
build/
.cache/
.parcel-cache/
tmp/
temp/
*.tmp

# Jest / test outputs
jest-cache/
jest-results/

# TypeScript (if added later)
*.tsbuildinfo

# OS / Editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Optional local config
*.local

# ESLint cache
.eslintcache

# PM2 / process manager
pm2.log

# Uploads (keep placeholder file if needed)
uploads/
!uploads/.gitkeep

# Docker artifacts
.docker/

# Lock files (keep only the one you use)
package-lock.json
yarn.lock
pnpm-lock.yaml

infrastructure

# Ignore lock files above only if you intentionally don't want them versioned.
# Remove the ones you actually want to keep under VCS.

# End
